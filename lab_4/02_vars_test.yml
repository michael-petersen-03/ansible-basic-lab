---
- name: Test Group and Host Variables
  hosts: nordics
  gather_facts: false

  tasks:
    - name: Show all variables
      ansible.builtin.debug:
        msg:
          - "Host: {{ inventory_hostname }}"
          - "Company: {{ company | default('not set') }}"
          - "Managed by: {{ managed_by | default('not set') }}"
          - "Role: {{ role | default('not set') }}"
          - "Environment Type: {{ env_type | default('not set') }}"
          - "Datacenter: {{ datacenter | default('not set') }}"
          - "Timezone: {{ timezone | default('not set') }}"
          - "Region: {{ region | default('not set') }}"
          - "NTP Server: {{ ntp_server | default('not set') }}"
          - "Backup Schedule: {{ backup_schedule | default('not set') }}"
          - "App Port: {{ app_port | default('not set') }}"
